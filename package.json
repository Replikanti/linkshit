{
  "name": "linkshit",
  "version": "0.3.36",
  "description": "Linkshit — auto-scroll your LinkedIn feed and surface posts matching your criteria via a local LLM.",
  "main": "host.js",
  "scripts": {
    "start": "node host.js",
    "check": "node --check host.js && node --check content.js && node --check background.js",
    "validate": "node -e \"['manifest.json','package.json'].forEach(f => JSON.parse(require('fs').readFileSync(f,'utf8')))\"",
    "lint": "eslint .",
    "pack": "node scripts/pack.js",
    "smoke": "node scripts/smoke-host.js",
    "audit": "npm audit --audit-level=high"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "eslint-plugin-unicorn": "^64.0.0",
    "globals": "^17.6.0",
    "web-ext": "^10.1.0"
  }
}
